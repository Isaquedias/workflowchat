import { Request, Response } from "express";


export default class ProcessFlowController {

    processFlow(request : Request, response : Response) {
      
        const drawFlowData = request.body;

        
        
    }
}